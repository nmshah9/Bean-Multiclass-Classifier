"""
Makes `src` an importable Python package so every module in this project
can use clean absolute imports like `from src.config import ...` instead of
fragile relative imports that break depending on which folder you run from.
"""
